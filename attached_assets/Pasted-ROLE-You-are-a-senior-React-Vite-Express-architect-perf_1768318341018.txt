ROLE: You are a senior React + Vite + Express architect performing a read-only–safe CMS/frontend separation fix.

CRITICAL CONSTRAINTS (DO NOT VIOLATE):
	•	❌ Do NOT change login, auth, session, or token logic
	•	❌ Do NOT change Admin routes, AdminAuthProvider logic, or auth callbacks
	•	❌ Do NOT introduce new redirects
	•	❌ Do NOT delete working CMS renderers
	•	❌ Do NOT change database schemas or Supabase queries
	•	❌ Do NOT touch server routing except where explicitly required

PRIMARY GOAL:
Ensure all public product pages render safely for non-admin users, while admin editing remains fully functional, and CMS-first pages continue to work.

⸻

TASKS (EXECUTE IN THIS ORDER)

1️⃣ Fix EditablePageWrapper (REQUIRED)
	•	Update client/src/components/admin/EditablePageWrapper.tsx
	•	Replace any usage of useAdminAuthContext() with useOptionalAdminAuth()
	•	Public users must never crash due to missing AdminAuthProvider
	•	Admin users must still see edit controls
	•	No UI or behavior regressions allowed

⸻

2️⃣ Verify CMS-first Routing (DO NOT CHANGE ROUTES)
	•	/cms/* must render using EconomyShedWorkingCopyRenderer
	•	CmsFirstPage.tsx must not import CmsPageRenderer
	•	CmsPageRenderer.tsx must remain deleted

⸻

3️⃣ Verify Legacy Product Pages
	•	Pages like:
	•	/types/basic-storage/economy-shed
	•	/types/deluxe-storage-cabins/cabin
	•	/types/garages-carports/garage
	•	Must render without admin providers
	•	Must not show admin UI to public users
	•	Must not throw auth errors

⸻

4️⃣ Server Safety Check (NO NEW ROUTES)
	•	Do not add app.get("*") or wildcard routes
	•	Ensure Express is not registering invalid * paths
	•	Keep current Vite dev / production behavior intact

⸻

5️⃣ Verification Checklist (MANDATORY)

Confirm ALL of the following:
	•	✅ npm run build passes
	•	✅ No imports of useCmsPage exist
	•	✅ No imports of CmsPageRenderer exist
	•	✅ No auth-related runtime errors for public users
	•	✅ Admin editing still works when logged in
	•	✅ No redirects were added
	•	✅ No server crashes on Railway

⸻

IMPORTANT:
If a file is already correct, do not modify it.
Make the minimum changes required to satisfy the goals above.

When finished, output:
	•	A list of files changed
	•	Why each change was required
	•	Explicit confirmation that auth/login logic was untouched