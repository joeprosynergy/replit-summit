You are performing a STRICT, READ-ONLY ARCHITECTURE FIX based on the provided CMS Architecture Read-Only Audit Report.

CRITICAL RULES (DO NOT VIOLATE):
- DO NOT change login, auth, or admin business logic
- DO NOT modify AdminAuthProvider, AdminLogin, or auth callbacks
- DO NOT refactor DynamicPage.tsx beyond import fixes
- DO NOT invent new CMS systems or abstractions
- DO NOT add new CMS renderers
- DO NOT change database schemas or Supabase logic
- DO NOT change routing structure beyond what is explicitly listed
- DO NOT reintroduce CmsPageRenderer.tsx
- DO NOT remove EconomyShedWorkingCopyRenderer.tsx
- DO NOT change contentMigration, canonicalization, or admin tooling

OBJECTIVE:
Fix the build and CMS-first routing using the EXISTING, WORKING CMS renderer and eliminate broken imports exactly as described in the audit.

PERFORM ONLY THE FOLLOWING ACTIONS:

1. FIX CmsFirstPage.tsx
   - Replace any import of CmsPageRenderer with EconomyShedWorkingCopyRenderer
   - Render EconomyShedWorkingCopyRenderer with pageSlug
   - Do not add new logic

   Final content MUST be:
   ---
   import { useParams } from "react-router-dom";
   import { EconomyShedWorkingCopyRenderer } from "@/components/EconomyShedWorkingCopyRenderer";

   const CmsFirstPage = () => {
     const { "*": fullPath } = useParams();
     const slug = fullPath || "";
     return <EconomyShedWorkingCopyRenderer pageSlug={slug} />;
   };

   export default CmsFirstPage;
   ---

2. DELETE the broken file:
   - client/src/components/CmsPageRenderer.tsx
   This file references non-existent dependencies and must be removed.

3. FIX Admin context crash SAFELY:
   - Create a new hook:
     client/src/contexts/useOptionalAdminAuth.ts
   - This hook must safely return admin defaults if no provider exists
   - It MUST NOT change existing AdminAuthProvider behavior

   Required implementation:
   ---
   import { useContext } from "react";
   import { AdminAuthContext } from "@/contexts/AdminAuthContext";

   export function useOptionalAdminAuth() {
     return useContext(AdminAuthContext) ?? {
       isAdmin: false,
       isRevalidating: false,
       user: null,
     };
   }
   ---

4. UPDATE EconomyShedWorkingCopyRenderer ONLY to:
   - Replace useAdminAuthContext with useOptionalAdminAuth
   - Change import and hook call ONLY
   - Do NOT modify rendering logic, admin tools, or CMS logic

5. VERIFY:
   - No remaining imports of CmsPageRenderer
   - No imports of useCmsPage
   - No build references to missing files
   - DynamicPage.tsx remains functionally unchanged

FINAL STEP:
- Report EXACT files changed or deleted
- Confirm that `npm run build` will pass
- Do NOT suggest further refactors

If any step cannot be completed safely under these constraints, STOP and report why.