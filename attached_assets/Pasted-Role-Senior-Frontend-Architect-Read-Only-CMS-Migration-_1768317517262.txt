Role: Senior Frontend Architect (Read-Only CMS Migration)

Objective:
Fix broken product pages caused by partial CMS-first migration without changing authentication, login logic, or admin permissions.

⸻

🚨 ABSOLUTE RULES (DO NOT VIOLATE)
	1.	DO NOT modify or refactor:
	•	Authentication logic
	•	Login / logout flows
	•	Admin role checks
	•	Supabase auth configuration
	•	Admin routes or admin pages
	2.	DO NOT add new CMS features
	3.	DO NOT migrate additional pages to CMS-first
	4.	DO NOT introduce new redirects
	5.	DO NOT change database schemas

⸻

🎯 ALLOWED SCOPE (ONLY THESE FILES MAY CHANGE)

You MAY:
	•	Create one optional admin context hook
	•	Modify EditablePageWrapper to make admin usage optional
	•	Fix imports that currently crash builds
	•	Remove dead or broken CMS files

You MAY NOT touch:
	•	Admin login pages
	•	Auth providers
	•	Supabase client initialization

⸻

🛠️ REQUIRED FIXES (IN THIS ORDER)

Step 1 — Optional Admin Context
Create:

client/src/contexts/useOptionalAdminAuth.ts

This hook must:
	•	Return { isAdmin: false } if no provider exists
	•	NEVER throw
	•	NEVER redirect

⸻

Step 2 — Make EditablePageWrapper Safe for Public Pages
File:

client/src/components/admin/EditablePageWrapper.tsx

Required changes:
	•	Replace any use of useAdminAuth with useOptionalAdminAuth
	•	Editing UI must render only if isAdmin === true
	•	Public users must receive read-only content
	•	No admin context → no crash

⸻

Step 3 — Fix CMS-First Entry Point
File:

client/src/pages/CmsFirstPage.tsx

Must:
	•	Use EconomyShedWorkingCopyRenderer
	•	NOT import CmsPageRenderer

⸻

Step 4 — Delete Broken CMS Files
Remove ONLY if present:

client/src/components/CmsPageRenderer.tsx

(This file references non-existent hooks and must not be re-created.)

⸻

Step 5 — Verification (MANDATORY)
You MUST verify:
	•	npm run build passes
	•	/cms/economy-shed-working-copy renders
	•	Product pages render without admin login
	•	Admin pages still work unchanged

⸻

🧪 SUCCESS CRITERIA
	•	No white screens
	•	No auth errors
	•	No new redirects
	•	No Supabase calls added to public rendering
	•	CMS-first and legacy pages both function

⸻

If any step would violate these rules, STOP and explain instead of changing code.