You are updating a custom website + CMS app to correctly use an updated Supabase schema.

IMPORTANT
	•	All table names, field names, and function names below are PLACEHOLDERS
	•	You MUST map them to the existing names already used in this codebase
	•	Do NOT invent new tables or concepts

⸻

DATABASE CHANGES THAT ALREADY EXIST

The Supabase database has been updated with:
	•	section_content.page_id (UUID foreign key to page)
	•	page_content.layout_config (JSONB)
	•	page_content.is_canonical (boolean)

Existing fields like page_slug still exist for backward compatibility.

⸻

REQUIRED GOAL

Update the application so that:
	•	page_id is the primary relationship between pages and sections
	•	page_slug is treated as derived / display-only
	•	All layout and styling is read from and written to layout_config
	•	Duplication, editing, and rendering use canonical CMS data

⸻

STEP 1 — UPDATE DATA ACCESS LAYER

Modify all data queries so that:
	•	Sections are fetched by page_id, NOT by page_slug
	•	Writes to sections always include page_id
	•	Slug changes do NOT affect section relationships

❌ Do not remove page_slug yet
❌ Do not break existing pages

⸻

STEP 2 — PERSIST LAYOUT & STYLES

Update the editor so that:
	•	All page-level layout and styling (background colour, spacing, layout type, theme)
are read from layout_config
	•	Any layout/style change made in the editor is saved back to layout_config
	•	No layout defaults are applied at render-time without being persisted

After this change, rendering must depend only on database state.

⸻

STEP 3 — CANONICALIZATION LOGIC

Implement or update a function conceptually named:

ensureCanonicalPage(PAGE_IDENTIFIER)

This function MUST:
	•	Detect if is_canonical is false
	•	If false:
	•	Extract all implicit runtime defaults
	•	Populate layout_config
	•	Ensure all sections are linked via page_id
	•	Mark is_canonical = true

This function MUST be called before:
	•	duplication
	•	advanced editing

⸻

STEP 4 — UPDATE DUPLICATION LOGIC

Update the duplication feature so that:
	•	Duplication ONLY runs on canonical pages
	•	Page rows are cloned with a new ID
	•	layout_config is copied EXACTLY
	•	Sections are duplicated and re-linked using the NEW page_id
	•	Manual slug / URL override continues to work exactly as before
	•	No data is shared between original and duplicate

⸻

HARD CONSTRAINTS (FAIL IF VIOLATED)
	•	Original pages must NEVER be modified
	•	UI behaviour must not change
	•	Auth logic must not change
	•	No fallback defaults allowed during duplication
	•	Slug changes must not break editability

⸻

OUTPUT REQUIRED
	1.	Updated data access logic
	2.	Updated editor save/load logic
	3.	Updated canonicalization logic
	4.	Updated duplication logic
	5.	Brief explanation of what changed and why

⸻

VALIDATION (MUST PASS)

After changes:
	•	Existing pages still render correctly
	•	Duplicated pages:
	•	visually match originals
	•	retain background colours
	•	open fully editable
	•	Editing a duplicate does NOT affect the original
	•	Slug changes do NOT break sections

If any requirement cannot be met, explain EXACTLY why and stop.