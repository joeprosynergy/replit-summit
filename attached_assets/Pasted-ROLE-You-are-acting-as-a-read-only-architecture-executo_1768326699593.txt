ROLE: You are acting as a read-only architecture executor.
You must follow the instructions exactly and must not refactor, re-architect, or “improve” anything outside the explicit scope below.

⸻

🎯 OBJECTIVE (DO NOT DEVIATE)

Implement OPTION B: Lazy-load Admin UI so that:
	•	Public users do not download any admin code
	•	Admin editing continues to work exactly as before
	•	CMS-first pages remain functional
	•	Product pages remain functional
	•	Login, auth, Supabase, and routing logic remain 100% untouched

⸻

🚫 ABSOLUTE RULES (HARD CONSTRAINTS)

You MUST NOT:
	•	Modify any auth logic
	•	Modify Supabase clients, keys, or queries
	•	Modify server routes or middleware
	•	Modify AdminLogin, AuthCallback, AdminAuthContext behavior
	•	Modify CMS database structure
	•	Rename existing routes
	•	Add new dependencies
	•	Delete working files unless explicitly instructed
	•	“Optimize” unrelated code
	•	Introduce new CMS abstractions

If a task requires breaking any of the above rules → STOP and ASK FIRST.

⸻

✅ ALLOWED ACTIONS ONLY

You may:
	•	Create new files
	•	Modify imports
	•	Replace direct admin UI imports with lazy equivalents
	•	Add React.lazy / Suspense
	•	Move admin UI exports behind a single entry point

⸻

📌 EXACT TASKS TO PERFORM (IN ORDER)

TASK 1 — Create Admin UI entry point

Create this file exactly:

client/src/components/admin/AdminUI.tsx

import { AdminEditMode } from "./AdminEditMode";
import { InlineEditable } from "./InlineEditable";
import InlineEditableButton from "./InlineEditableButton";
import InlineEditableImage from "./InlineEditableImage";
import InlineEditableLink from "./InlineEditableLink";

export {
  AdminEditMode,
  InlineEditable,
  InlineEditableButton,
  InlineEditableImage,
  InlineEditableLink,
};


⸻

TASK 2 — Create lazy admin loader

Create this file exactly:

client/src/components/admin/useAdminUI.ts

import { lazy } from "react";

export const AdminUI = lazy(() =>
  import("./AdminUI").then((m) => ({ default: m }))
);


⸻

TASK 3 — Update EditablePageWrapper (CRITICAL)

Edit only this file:

client/src/components/admin/EditablePageWrapper.tsx
	•	Replace all direct admin UI imports
	•	Import:

import { Suspense } from "react";
import { useOptionalAdminAuth } from "@/contexts/useOptionalAdminAuth";
import { AdminUI } from "./useAdminUI";

	•	Wrap admin UI rendering exactly like this:

const { isAdmin } = useOptionalAdminAuth();

{isAdmin && (
  <Suspense fallback={null}>
    <AdminUI />
  </Suspense>
)}

	•	Do not alter logic outside admin rendering

⸻

TASK 4 — Update admin UI imports in pages

For files importing admin UI directly:

Replace:

import { InlineEditable } from "@/components/admin/InlineEditable";

With:

import { InlineEditable } from "@/components/admin/AdminUI";

This applies only to admin UI imports, not logic.

⸻

TASK 5 — VERIFY

After changes:
	•	Run npm run build
	•	Confirm:
	•	Public pages render
	•	CMS-first pages render
	•	Admin editing works after login
	•	No auth code was touched

⸻

🛑 OUTPUT FORMAT (MANDATORY)

At completion, output ONLY:
	1.	Files created
	2.	Files modified
	3.	Files not touched (auth confirmation)
	4.	Build result: PASS / FAIL

Do not include explanations unless an error occurs.

⸻

⚠️ FAILURE CONDITION

If you are unsure at any step:
	•	STOP
	•	ASK for clarification
	•	Do NOT guess